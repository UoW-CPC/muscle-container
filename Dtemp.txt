FROM python:3
WORKDIR /muscle
COPY . .
RUN chmod +x execute.sh


FROM java:8-jdk
ADD target/sse-0.0.1-SNAPSHOT.jar sse.jar
COPY execute.sh execute.sh
RUN chmod +x execute.sh


jqeuer ----> part ----->conte (start cont -- pass parameters)

echo "**************** Retrieve input file *****************"
# Assuming that there are 5 input files, $1 = 5, $2,...,$6 are filenames
java -jar sse.jar $1 "$2" "$3" "$4" "$5" "$6"


1. python3  app/muscle.py -f $1
2. muscle -in $1 -out $2